# PRD — 절대 바뀌면 안 되는 것들 (Locked Principles)

> 출처: `CLAUDE.md` §0·§1, `CHARACTER_SYSTEM.md`, `DIALOGUE_SYSTEM.md`, `WHO_ATE_MY_CHEESECAKE_GAME_GUIDE_KO.md`,
> `BACKLOG.md`/`HANDOFF.md`, 커밋 `c0eeed5`·`855a91b`·`39490ee`·`faad038`.
> 이 문서는 새 결정을 담지 않는다. **이미 여러 문서·커밋에 "정본"·"기준 문서"·"금지"로 못박힌 것만 한 곳에 모은다.**
> 구현이 이 원칙과 충돌하면 §7의 우선순위를 따른다.

---

## 1. 제품 목적 (고정)

> "게임 목적은 SPUM 엔진 + SAM 마케팅 데모다. 캐릭터 움직임과 대화가 전부 SPUM/SAM 안에서
> 나와야 한다. JS 로 직접 그리거나 움직이는 백업 경로는 걷어내는 방향으로 간다." — `HANDOFF.md`

- 맵·타일·캐릭터·애니메이션·길찾기 — **SPUM 엔진**이 그린다. 자체 렌더링 백업 경로 금지.
- NPC 대사, NPC끼리 주고받는 소문 한 마디까지 — **SAM**이 짓는다. 정적 대사 배열로 대체 금지.
- 과제·채점 프레이밍 금지 (`855a91b`) — 이건 팀이 실제로 쓰는 마케팅 데모지 제출용 산출물이 아니다.

---

## 2. 캐릭터 시스템 (기준 문서: `CHARACTER_SYSTEM.md`)

### 2.1 서사 최소화 — 절대 원칙
캐릭터에게 방대한 배경 설정·대사 데이터베이스·고정 스토리라인을 **임의로 추가해서는 안 된다** (§1).
필요한 정보는: 나이·직업·생활 형태·생활 루틴·아주 적은 과거·성격적 경향·관심사·욕구·민감한 부분·관계 성향, 그 이상은 넣지 않는다 (§2.1, §15).

**이유가 원칙이다 (§2.2):** 정보를 많이 넣을수록 NPC 대사가 "저장된 정보 검색 → 반응 → 재검색"의 닫힌 루프가 된다.
대화 문제가 생겼을 때 캐릭터 데이터를 추가하는 것이 아니라, 문맥/화행/관계/감정/기억을 먼저 의심한다 (§15, §16-금지6).

### 2.2 구현 금지 목록 (§16, 문구 그대로)
1. 상세한 연대기(유년~현재 타임라인)
2. 캐릭터당 수십 개 개인 사건
3. 캐릭터별 고정 Topic ("이 캐릭터는 반드시 이 주제를 이야기한다")
4. 고정된 반응 (같은 질문 = 같은 답)
5. Lore Retrieval 중심 설계 (과거 이야기를 계속 검색해 대화를 이어가는 구조)
6. 설정 추가로 모든 대화 문제를 해결하려는 접근

### 2.3 성격은 규칙이 아니라 경향(Tendency)
"항상 비꼬는 말투로 대답한다" 같은 절대 규칙 금지. "~하는 경향이 있다"로만 정의한다 (§5.4, `DIALOGUE_SYSTEM.md` §8.1).

### 2.4 MBTI — 출발점이지 규칙이 아니다 (`39490ee`)
6명의 MBTI는 정본에 못박혀 있다: 하루 ISTP · 미누 INFJ · 루루 ESFP · 피치 ESTJ · 코코 INTP · 루비 ENFP.
단, **"MBTI는 출발점, 성격적 경향이 실제 기준. 어긋나면 경향을 따른다."** — MBTI 넉 자가 §5.4가 경계하는
고정 성격으로 굳어지게 해서는 안 된다.

### 2.5 관계는 상황에 따라 변하는 상태
캐릭터 간 관계는 구체적 공동 과거가 아니라 관계적 경향(친밀도·신뢰·존중·긴장·의심 등)으로 정의하고,
게임 진행에 따라 자연스럽게 변해야 한다. **앞으로의 관계 변화를 미리 정해 두어서는 안 된다** (§7).

### 2.6 Player / NPC 구조 — 고정
- 6명 중 플레이어가 고른 한 명 = Player, 나머지 5명 = AI NPC.
- Player는 캐릭터 정체성(가치관·욕구·관심사·관계·기억·경험·판단 경향)은 유지하되, **말투·표현 방식은 AI가 강요해서는 안 된다** (§8).
- 반대로 **AI가 플레이어가 무슨 말을 할지 대신 결정해서는 안 된다** (§8).

---

## 3. 대화 시스템 (기준 문서: `DIALOGUE_SYSTEM.md`)

### 3.1 사실 계층과 표현 계층의 분리 — 가장 중요한 아키텍처 원칙 (§21)
> **"사실은 게임 엔진이 소유하고, 말은 SAM이 만든다."**

- 게임 엔진이 소유: 실제 시간·장소·동선·목격·알리바이·참/거짓·퍼즐 정답·전언의 실제 출처.
- SAM이 소유: 성격·말투·감정·관계·현재 대화에 맞춘 자연스러운 표현.
- **SAM이 게임의 규칙이나 정답을 임의로 결정해서는 안 된다.**

### 3.2 화행(Speech Act) 적합성
질문엔 실제로 답해야 한다. "왜 늦었어?"에 "나는 커피를 좋아해"처럼 문장은 자연스러워도 화행에
안 맞는 응답은 **대화 실패**로 취급한다 (§6, §10).

### 3.3 문맥 연속성
각 발화를 독립 입력으로 처리하지 않는다. 현재 주제·미해결 질문·최근 발언·관계 상태를 유지한다 (§9).
화제 전환과 이전 주제 복귀가 가능해야 하며, 전환은 무작위가 아니라 상대 발화·목적·관심·상황에 따라 일어나야 한다 (§10).

### 3.4 지식의 경계와 출처(Provenance)
- 모르는 정보를 그럴듯하게 지어내지 않는다 (§11).
- "무엇을 아는가 + 어떻게 알게 되었는가"를 함께 관리한다 (§12).
- 직접 경험과 전언을 구분한다 — "내가 봤어" 대신 "B에게 들었어" (§13).
- 정보가 캐릭터 사이를 이동해도 출처는 유지되어야 한다 (§17).

### 3.5 자기 발화·목격 검증
NPC가 "내가 아까 그렇게 말했어" / "내가 직접 봤어"라고 주장하려면 실제 대화 기록·기억에 근거가 있어야 한다.
플레이어가 하지 않은 말을 했다고 우기면 NPC는 그 전제를 그대로 받아들이지 않는다 (§14). 검증 실패 응답은 수정/재생성한다 (§22).

### 3.6 기억은 대화 기록의 복사가 아니다
모든 발화를 영구 저장하지 않는다. "대화가 자신에게 무엇을 남겼는지"만 선택적으로 기억한다 (§15).

### 3.7 NPC ↔ NPC 정보 전파 — 출처 보존이 핵심
NPC끼리도 정보를 주고받고 의심하고 기억해야 한다. 이건 단순 "소문 기능"이 아니라
**"정보가 캐릭터 사이를 이동할 때 출처가 보존되는 구조"**다 (§17). 이 전파는 사건 당사자(하우스메이트)로
한정되지 않고 동네 사람에게도 같은 구조로 적용된다 (§17.1).

### 3.8 사실 전달과 열린 추측은 다른 문제 (§17.2)
하우스메이트끼리 "범인이 누굴까" 짐작을 나눌 수는 있지만, **SAM이 "범인은 ○○다"처럼 확정해서
말하게 해서는 안 된다.** "○○ 좀 수상하지 않아?" 정도의 의심까지만 허용.

### 3.9 한국어는 영어식 논리 구조로 단순화하지 않는다
`-면` = `if` 같은 일대일 대응 가정 금지. 형태·문법은 문맥·화행·관계·상황·캐릭터 지식과 함께 해석한다 (§4.1).

### 3.10 응답 검증은 항상 거친다
생성 → 검증(출처 날조·미경험 주장·문맥 모순·질문 회피 여부 등) → 문제 있으면 수정/재생성. 자연스러움만으로 바로 출력하지 않는다 (§22).

---

## 4. 게임 규칙 — 확정된 사실 구조 (`WHO_ATE_MY_CHEESECAKE_GAME_GUIDE_KO.md`)

- 밤은 6개 시각(21·22·23·00·01·02시), 방은 8개(부엌·식당·거실·서재·데크·운동장·텃밭·헛간).
- 같은 시각·같은 방이면 서로 목격한 것 — 목격 판정은 이게 전부다.
- 범행은 **범인이 부엌에 혼자 있던 시각**에 일어난다 (그 시각만 목격자가 없다).
- **거짓말은 딱 세 종류뿐이다**: ① 방패(안 먹은 이유, 일부는 거짓 — 깨는 실마리는 반드시 판 안에 존재) ② 범인의 알리바이 거짓말 ③ 공범의 거짓 목격.
  > "이 밖의 거짓은 없다. NPC는 없는 시각·장소·사람을 지어내지 못한다 — 엔진이 검사해서 막는다."
- 플레이어가 고른 캐릭터는 범인 후보에서 자동 제외된다.
- 소문은 근접(2.5칸)·시간(4.2초)에 따라 번지고, 출처를 달고 전파된다. **엿들은 소문은 증언판에 안 올라간다** — 직접 물어서 그 사람 입으로 들어야 확정된다.
- 네가 누군가에 대해 캐물은 행위 자체도 소문이 되어 퍼진다("하루가 루비 행적을 캐고 다니더라").

---

## 5. UI/UX 프레이밍 — 못박힌 문구 규칙

- **"미리 정해진 동선"·"배정된 방" 같은 표현 금지** (`c0eeed5`). 사실 자체(시각이 바뀌면 방을 옮긴다)는
  맞지만, 플레이어가 "그럼 대사도 정해진 거 아냐?"로 오해할 수 있어 이 프레이밍은 뺐다.
  NPC 자리는 `round.mjs`의 `makePaths()`가 판마다 난수로 새로 굴리는 것이지 대본이 아니다.
- 과제/채점 관련 프레이밍 금지 (`855a91b`).
- 플레이어가 고른 캐릭터의 **MBTI·경향·말투는 SAM에 전달되지 않는다** — 뭐라고 말하든 자유.
  다만 **나이·직업·생활은 남는다** — 식구들은 네가 누군지 안다. 이 둘의 구분(서사는 남고 말투는 풀린다)이 게임 안내서에 명시된 계약이다.

---

## 6. 보안 (`CLAUDE.md` §1, 커밋 `faad038`)

- **SAM 키는 어떤 파일에도 쓰지 않는다.** 전체든 접두사든. `.env`(gitignore) 또는 환경변수만.
- `claude mcp add --scope project` 금지 — `.mcp.json`에 키가 커밋됨. `local`/`user`만 사용.
- 회사 제공 자료(회사소개서 등) 커밋 금지 — gitignore 폴더(`_local/`)로.
- 공개 배포 시 `/api/import`(인증 없이 파일 쓰기)는 프로덕션에서 반드시 막는다 — 로컬 개발 전용 도구.
- 공유 SAM 키 남용 방지 — SAM 프록시에 IP당 분당 호출 제한을 둔다.

---

## 7. 충돌 시 우선순위 (`CHARACTER_SYSTEM.md` §17, 원문 그대로)

캐릭터 시스템 구현이 이 문서의 원칙과 충돌할 경우:

1. Open-ended Conversation
2. Contextual Coherence
3. Speech Act Compatibility
4. Emergent Interaction
5. Player Freedom
6. Minimal Character Constraints
7. Dynamic Relationships

> "캐릭터를 더 풍부하게 보이게 하겠다는 이유로 이 원칙들을 희생해서는 안 된다."

---

## 8. 이 문서의 위치

```text
CHARACTER_SYSTEM.md   → 캐릭터는 어떤 존재인가 (정본)
DIALOGUE_SYSTEM.md    → 그 캐릭터는 어떻게 대화하는가 (정본)
GAME 엔진(round.mjs 등) → 게임 세계에서 무엇이 실제 사실인가 (정본)
CLAUDE.md             → 작업 중 반복해서 당한 함정과 확인된 규칙
PRD_LOCKED_PRINCIPLES.md (이 문서) → 위 정본들에서 "절대/금지/못박는다"로 표시된 것만 모은 요약
```

새 기능을 넣기 전에 이 문서와 충돌하는지 먼저 확인한다. 충돌하면 §7 우선순위를 따르고,
그래도 예외가 필요하면 **정본 문서(`CHARACTER_SYSTEM.md`/`DIALOGUE_SYSTEM.md`)를 먼저 고친 뒤** 코드를 바꾼다 — 반대 순서로 하지 않는다.
